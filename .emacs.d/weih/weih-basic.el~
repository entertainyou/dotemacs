;; HUANG Wei's emacs setting

(setq user-full-name "entertainyou")
(setq user-mail-address "grubbyfans@gmail.com")

(setq major-mode 'text-mode)

(setq fill-column 80)

(require 'color-theme)
(color-theme-initialize)
(require 'color-theme-blackboard)

(color-theme-blackbord)

(menu-bar-mode -1)
(tool-bar-mode -1)

(provide 'weih-basic)
